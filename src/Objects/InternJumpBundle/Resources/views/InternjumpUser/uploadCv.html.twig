{% extends '::base.html.twig' %}
{% block title %}Cv Upload{% endblock %}


{% block pageTitle %}Cv Upload{% endblock %}

{% block body %}
{% if form_errors(form) %}
<div class="alert alert-error">
    <button data-dismiss="alert" class="close" type="button"> &times; </button>
    {{ form_errors(form) }}
</div>
{% endif %}
<div class="container Main-content">
    <div class="Student">
        <h3>Cv Upload</h3>
        <p></p>
        <div class="row-fluid show-grid">
            <div class="span12">
                <form action="{{ url('upload_cv') }}" method="post" {{ form_enctype(form) }} id="imageUploadForm">
                    <fieldset>
                        <legend></legend>
                        <div class="Sign-Up">
                            <ul>
                                <li>Download Cv Template From <a href="{{asset('cvTemplate.doc')}}">Here</a></li>
                                <li>Fill The Template With Your Data</li>
                                <li>Upload The Template</li>
                            </ul>
                            <div class="controls">
                                <label>User Name</label>
                                {{ form_widget(form.file) }}
                                {% if form_errors(form.file) %}
                                <div class="alert alert-error">
                                    <button type="button" class="close" data-dismiss="alert"> × </button>
                                    {{ form_errors(form.file) }}
                                </div>
                                {% endif %}
                            </div>
                            <div class="controls  row-fluid show-grid">
                                <div class="span6">
                                    {{ form_rest(form) }}
                                    </div>
                                    <div class="span6">
                                        <button class="btn btn-primary pull-right" type="submit">Upload</button>
                                    </div>
                                </div>
                        </fieldset>
                    </form>
                </div><!--Sign-Up-->
            </div>
        </div><!--Sign-Up-->

    </div><!---Student --->
</div>

{% endblock %}